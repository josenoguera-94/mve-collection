{
  "name": "Databricks Local Dev",
  "dockerComposeFile": [
    "../docker-compose.yml"
  ],
  "service": "spark",
  "workspaceFolder": "/app",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  },
  "postCreateCommand": "pip3 install uv && uv venv --clear --python /usr/bin/python3 && uv sync",
  "remoteUser": "root"
}